import 'package:strategy/controller.dart';
// import 'package:strategy/web_client.dart';
// import 'dart:io';


void main(List<String> args) {
  Controller().start();
  // var a = WebClient(WebClient.defaultServer);
  // var pid = await a.getPID("Smart");
  // stdout.writeln(pid['pid']);
  // var response = await a.getAckMove('636475ae2110d', '3,3');
  // stdout.writeln(response);
}
